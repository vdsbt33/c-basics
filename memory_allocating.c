#include <stdio.h>
#include <stdlib.h>

typedef enum { false, true } bool;

int main(void) {
    bool keepExecuting = true;
    unsigned int* numbersToSum = malloc(sizeof(unsigned int));
    int result;

    for (int i = 0; i < 5; i++) {
        
        numbersToSum

    }

    printf("Result = %i", result);
    free(numbersToSum);

    return 0;
}
